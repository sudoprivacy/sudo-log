[project]
name = "sudo-log-sdk"
version = "0.1.0"
description = "Sudo Log batch ingest SDK for Python."
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
